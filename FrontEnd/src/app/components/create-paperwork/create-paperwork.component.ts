import { Component, OnInit } from '@angular/core';

/**
 * CreatePaperworkComponent is responsible for the creation of new paperwork.
 * It initializes the component when needed but currently does not contain any specific logic in the `ngOnInit` lifecycle hook.
 */
@Component({
  selector: 'app-create-paperwork',
  templateUrl: './create-paperwork.component.html',
  styleUrls: ['./create-paperwork.component.css']
})
export class CreatePaperworkComponent implements OnInit {

  /**
   * Initializes the component.
   *
   * @returns {void}
   */
  ngOnInit(): void {}
}
